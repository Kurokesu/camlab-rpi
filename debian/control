Source: camlab-rpi
Section: video
Priority: optional
Maintainer: Kurokesu <info@kurokesu.com>
Build-Depends: debhelper-compat (= 13)
Standards-Version: 4.7.0
Homepage: https://github.com/Kurokesu/camlab-rpi
Rules-Requires-Root: no

Package: camlab-rpi
Architecture: all
# python3 is declared here rather than rendered, an interpreted tree
# needs it and deps.sh leaves it to Raspberry Pi OS
Depends: ${misc:Depends},
 python3,
 ${camlab:AppDepends},
 ${camlab:StackDepends}
# camlab-setup installs drivers after kernel.sh prunes the
# sibling kernel flavor.
Suggests: ar0234-rpi-dkms,
 ar0822-rpi-dkms,
 imx585-rpi-dkms
Description: Kurokesu camera test bench tool for Raspberry Pi
 Single-operator bench GUI for validating Kurokesu Raspberry Pi camera
 modules: live viewfinder, sensor selection (writes the dtoverlay) and
 signal-integrity surfacing. Runs fullscreen under a Cage kiosk.
 .
 Installs the application tree to /opt/camlab and restarts a running
 camlab.service. Sensor drivers, service wiring, boot tuning and the
 read-only overlay stay with camlab-setup, which the operator runs once
 after install.
