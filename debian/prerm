#!/bin/sh
set -e

# Drop bytecode postinst generated so removal leaves no unowned files.
case "$1" in
    remove)
        find /opt/camlab/camlab -type d -name __pycache__ -prune -exec rm -rf {} + 2>/dev/null || true
        ;;
esac

#DEBHELPER#

exit 0
