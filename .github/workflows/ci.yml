# SPDX-FileCopyrightText: 2026 UAB Kurokesu
# SPDX-License-Identifier: GPL-3.0-or-later

name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Ruff
        run: |
          pip install ruff
          ruff check camlab tests deploy
          ruff format --check camlab tests deploy

      - name: Shellcheck
        run: git ls-files '*.sh' | xargs shellcheck

      - name: REUSE lint
        uses: fsfe/reuse-action@v6

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install test deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libegl1 libgl1 libxkbcommon0
          pip install pytest pyyaml numpy PyQt6

      - name: Compile python
        run: python3 -m compileall -q camlab deploy

      - name: Parse registry yaml
        run: |
          python3 -c "import yaml; yaml.safe_load(open('camlab/data/sensors.yaml'))"
          python3 -c "import yaml; yaml.safe_load(open('camlab/data/dsi_panels.yaml'))"

      - name: Pytest
        run: pytest
